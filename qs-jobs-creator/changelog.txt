Version 2.1.1
--------UPDATES------------------------
All Files
--------WHATS NEW------------------------
Added new translations and fixed existing ones.
Migrated more configurations from config.lua to the interface and config.json.
(Changing these configs in the interface no longer requires a server restart).
Fixed auto installation on framework.
Fixed several interface issues.
Improved the live config interface for smoother use.
Fixed resource usage — script now idles at 0.0 resmon with no markers nearby and ~0.3/0.4 resmon when markers are nearby.
Fixed okokBanking integration to correctly retrieve society money.
Fixed Owned Garage issues.
